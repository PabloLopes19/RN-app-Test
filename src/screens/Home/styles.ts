import styled from "styled-components";

import { light } from '../../styles/global';

export const Title = styled.Text `
    color: ${light.fdlightTitle};
    font-size: 18px;
    font-weight: bold;
`;